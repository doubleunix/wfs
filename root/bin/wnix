#!/bin/sh

OPTIONS=(--print-build-logs)
NIX=(nix ${OPTIONS[@]})

if [[ $1 == install ]]; then
    set -- "$1" $(shift; for arg do printf 'nixpkgs#%s ' "$arg"; done)
    ${NIX[@]} profile $@
else
    ${NIX[@]} $@
fi
